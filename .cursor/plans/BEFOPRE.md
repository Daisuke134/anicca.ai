いい質問ふたつ、分けて答えるね。

---

# 1. Centaur / Expected Parrot をどう使うか（結論から）

### 1-1. Centaur を「本番の中核」には **使わない**

ちゃんと調べたうえで結論から言うと：

> **Centaur は「研究用のサブ脳」としてだけ使い、
> 本番のシミュレーション脳は GPT-系（GPT‑5.1）＋ Expected Parrot にする。**

理由は現実的にかなりはっきりしている。

* Centaur は Llama‑3.1 70B ベースのモデルで、
  README 上でも **4bit でも 80GB VRAM、通常モードで少なくとも 160GB VRAM が必要** と明記されている。([GitHub][1])
  → 自前ホスティングは事実上 H100×2 クラスが前提。
* 研究側では「どんな心理実験でも自然言語で書けば人間行動をかなりの精度で当てる」
  という超強力なモデルだけど、
  生活習慣やスマホ行動への応用はまだ論文レベルでの検証しかない。([Nature][2])

スマホアプリのバックエンドとして毎日バンバン叩くには重すぎるし、
コスト的にも現実的じゃない。

なので：

* **Anicca のプロダクト用シミュレーション脳**
  → OpenAI の GPT‑5.1（API）
* **研究用に一部のユーザー・一部の実験でだけ Centaur を使う**
  → 例えば Featherless などの Centaur ホスティングを叩いて、
  「同じプロンプトを Centaur と GPT‑5.1 の両方に投げて比較する」とか。([featherless.ai][3])

という二段構えにしておくのが一番バランスいい。

### 1-2. Expected Parrot は「RL の代わり」ではなく「シミュレーション環境」

Expected Parrot EDSL がやってくれるのは

* 「**実験の構造**（条件・質問・エージェント）を Python で宣言的に書く」
* それを大量の LLM エージェントに一気に流して、
* 結果を **再現性のあるデータセット** として返してくれる

という「実験オーケストレーター」。([GitHub][4])

ここには「方策を最適化するアルゴリズム」自体は入っていない。

なので構造はこうなる：

* **環境**:
  Expected Parrot + GPT‑5.1 で作った「ユーザー・デジタルツイン」
* **エージェント（方策）**:
  こっち側で実装する RL / バンディット（後で決める）
* **学習データ**:
  EP がくれる `state, action, simulated_reward` の束

つまり Expected Parrot は **RL を置き換える存在ではなく、
RL が走る“行動シミュレーション環境”** になる。

### 1-3. 「デジタルツインで RL」をどうやるか（ざっくりフロー）

1. そのユーザーの特徴（Big Five / MBTI / 行動傾向 / 理想・ストラグル）を supermemory にまとめる。([GitHub][5])

2. Expected Parrot EDSL で
   「23:15、ベッドで X を 40 分見ている」「明日は 7:00 に起きたい」みたいな
   状態テンプレを定義し、実際のログからパラメータを埋める。([docs.expectedparrot.com][6])

3. Nudge 候補（柔らかい/厳しめ/仏教メタ視点…）を全部列挙して、
   Twin(u) に「これを送られたら何をするか」「どう感じるか」を答えさせる。([GitHub][7])

4. その結果から `r̂(s,a)`（シミュレーション上の報酬）を作り、
   **RL（コンテキスト付きバンディット）で「Twin 上の最適ポリシー」を学習する。**

5. 学習したポリシーを本番のバンディットに埋め込み、
   実ユーザーのログからの差分でさらに微調整する。

こうすると

* 中身の RL はちゃんと数学的な方策改善
* Expected Parrot は「その RL が遊ぶサンドボックス」

という役割分担になる。

---

# 2. 「最高のスタック」を GitHub まで調べたうえで決める

ここからは完全に決め打ちでいく。

## 2-1. 全体アーキテクチャ（レイヤーごとに）

1. **ログ & Feature Store**

   * Supabase/Postgres（行動イベント・集約特徴を全部ここ）
2. **長期メモリ**

   * supermemory（MCP）
3. **高速検索**

   * Moss runtime
4. **シミュレーション & 実験**

   * Expected Parrot EDSL + GPT‑5.1
5. **オンライン方策学習（RL）**

   * `contextualbandits` ライブラリ
6. **対話・文面生成**

   * OpenAI GPT‑5.1 / Realtime API

この 6 つが中核。
以下、それぞれ GitHub / 論文を見たうえでなぜこれにしたか。

---

## 2-2. メモリ & 検索レイヤーの選定

### supermemory を中核メモリにする理由

* supermemory は「Memory エンジン + MCP サーバ」がセットになっていて、
  ChatGPT / Cursor / 他の MCP クライアントから同じ記憶を共有できる。([GitHub][5])
  → Anicca iOS / 開発用エージェント / 将来の Web UI で統一可能。
* 実際の導入事例で、mem0 から supermemory に移行したサービスが
  「安定性とレイテンシが大幅に改善した」とレポートしている。([Reddit][8])

mem0 も強力だけど、
「MCP 前提でいろんなクライアントから同じ記憶を見たい」
という Anicca の構想には supermemory の方が素直なので、
**長期メモリは supermemory で固定**してしまう。

### Moss を併用する理由

* Moss は「リアルタイム意味検索ランタイム」で、
  サブ 10ms の検索・即時インデックス更新を売りにしている。([usemoss.dev][9])

やりたいことは

* 音声対話 / Nudge 生成の瞬間に

  * 「最近の睡眠に関するエピソード」
  * 「ユーザーが ‘自分を律したい’ と話した発言」
  * 「直近 3 日の夜の後悔」

みたいなテキスト記憶を一瞬で引きたい。

supermemory を「正本の記憶庫」
Moss を「リアルタイム検索キャッシュ」として組み合わせる構成に決める。

---

## 2-3. シミュレーション・エンジン選定（EP 以外も含めて比較）

### Expected Parrot を採用する理由

* EDSL は「AI エージェントにアンケート・実験を投げるための DSL」。
  Python から実験を宣言的に書けて、結果は整ったデータフレームで返る。([GitHub][4])
* 調査用途だけでなく、**ユーザー・シミュレータとしての LLM 実験**にもフィットする。
* GitHub のコードを見ても、

  * 実験設計
  * 実行
  * 分析（集計・可視化）
    までパイプラインが揃っていて、
    自前で一から作るより明らかに早い。

他の候補も調べた：

* Lusifer（LLM-based user simulation for recommender systems）([GitHub][7])

  * LLM でユーザーのフィードバックをシミュレーションして RL を回す環境。
  * MovieLens 用に特化した構成で、
    「推薦アイテムに対する評価」を出すのがメイン。
  * Anicca の「生活行動 + Nudge」の方がタスクが広いので、
    Lusifer は**設計の参考**に留めて、
    コアには据えない。

* YuLan‑Rec / RecAgent / AgentSociety 系([GitHub][10])

  * どれも「多数ユーザーの推薦行動」や
    「社会全体シミュレーション」に強いフレームワーク。
  * 今欲しいのは「1 人のユーザーを深くシミュレート」する仕組みなので、
    スケールの方向が違う。
    → 直接使う必要はなく、
    「プロンプトデザインやユーザープロファイル更新の構造」を参考にする位置づけにする。

* Generative Agents / AI Town / SkyAGI / TinyTroupe([GitHub][11])

  * マルチエージェントの街 / ソーシャル環境を作るためのフレームワーク。
  * 「記憶 → 反省 → 計画 → 行動」というアーキテクチャは参考になる。
  * ただ、Anicca の第一段階でここまで重い環境を抱える必要はない。
    → 構造は真似しつつ、実装は Expected Parrot + 自前コードで十分。

**結論**
→ シミュレーション・オーケストレータは Expected Parrot に決める。
　他は「設計のインスパイア元」として扱う。

---

## 2-4. シミュレーション脳（LLM）は何を使うか

選択肢としては

1. Centaur 70B（Psych‑101 で finetune された認知モデル）([Hugging Face][12])
2. GPT‑5.1 などのフロントライン LLM
3. 8B〜14B クラスの軽量 OSS モデル（Llama 3.1 8B など）([Hugging Face][13])

今回の目的：

* ユーザーごとの **Nudge の差分評価** ができればよい
  （「この文言とこの文言で Twin がどう行動を変えるか」を比較したい）
* スマホアプリのバックエンドとして
  **現実的なコスト・レイテンシ** で回したい
* 精度は最終的に **実ユーザーのバンディット学習** で補正する

これを考えると、

* Centaur は「心理実験の再現」では最強だけど、
  80〜160GB GPU 前提はさすがに重すぎる。([GitHub][1])
* RL 研究でも、「普通の GPT‑系 LLM をユーザーシミュレータとして使って RL を回す」
  という論文がすでに複数出ていて、
  レコメンダの RL 学習をかなり改善できることが示されている。([arXiv][14])

なので Anicca のベースラインとしては

> **シミュレーション脳は GPT‑5.1（≒今使っているモデル）を使う。**
> Centaur は、将来「どうしても心理タスクの再現精度を上げたい研究実験」にだけ使う。

と決めるのが現実的かつ強い。

---

## 2-5. オンライン RL / バンディットの選定

ここは実際にコードとして入る部分。

候補を GitHub で見た上で、こう決める。

1. **contextualbandits**（David Cortes）([GitHub][15])

   * LinUCB / Thompson / LogisticUCB など定番のコンテキスト付きバンディットを実装。
   * Python ライブラリとしてシンプルに組み込める。
   * 離散アクション向けで、まさに「Nudge タイプ」最適化と相性がいい。

2. **CATX**（InstaDeep）([GitHub][16])

   * 連続アクション空間向け。
   * 今回の Nudge は「カテゴリの組み合わせ」なので、連続空間は不要。

3. **Vowpal Wabbit**([GitHub][1])

   * 大規模オンライン学習に実戦投入されているコンテキスト付きバンディット実装。
   * ただし CLI ベース＋学習パイプラインの構築がそれなりにヘビー。

Anicca 0.3〜0.4 の段階で求めるのは

* まず**ちゃんと動く**もの
* Python サービス内に**直接埋め込める**もの
* 離散アクション（Nudge 種類）にフォーカスしたもの

なので、

> **オンライン方策学習は contextualbandits を採用する。**

ユーザー数が数十万〜数百万になって
より大規模な分散オンライン学習が必要になったときに、
その時点で Vowpal Wabbit を検討すれば十分。

---

## 2-6. LLM 人間シミュレーション系の GitHub を踏まえた設計

さっき調べた human‑simulation 系のリポジトリは、
以下のように位置づける。

* **awesome-llm-human-simulation / LLM‑UM Reading list**
  → 常に最新の研究を追うための「文献インデックス」として使う。([GitHub][17])

* **Generative Agents / SkyAGI / AI Town / AgentSociety / YuLan‑OneSim**([GitHub][11])
  → マルチエージェント・社会シミュレーションに強い。
  → Anicca の「1 人のユーザー」にはスケールしすぎなので、

  * 記憶アーキテクチャ（Observation → Reflection → Planning）
  * 長期ゴールと短期行動の分離
    を設計のヒントとして使う。

* **Lusifer / RecAgent / LLM‑powered user simulator for RS**([GitHub][7])
  → 「ユーザープロファイルを LLM プロンプトとして持ち、
  推薦を投げてフィードバックを LLM で生成する」という構造が Anicca の nudge 実験とほぼ同じ。
  → プロンプトテンプレや「プロファイルの更新ルール」を真似する価値が高い。

**スタックとしては Expected Parrot + GPT‑5.1 を軸にしつつ、
上記リポジトリの設計を吸収する、という方針でいく。**

---

## 2-7. 最後に「1日の流れ」で全体像をもう一度まとめる

### ステップ 0: オンボーディング

* 理想の姿タグ
* Struggle タグ
  をタップで選択。
  データ接続（Screen Time / Health / Motion / X など）に同意。

### ステップ 1: 記憶と特徴量の構築

* 生の行動ログは Supabase/Postgres。
* 日次サマリや会話・ Nudge へのコメントは supermemory に格納。([GitHub][5])
* GPT‑5.1 を使って Big Five / MBTI / 価値観を推定し、
  プロファイル＋ツインのパラメータにする。
* Moss は、対話や Nudge の生成タイミングで
  supermemory と日次要約から関連エピソードをサブ 10ms で引く。([usemoss.dev][9])

### ステップ 2: 夜間のシミュレーションジョブ（EP + GPT‑5.1）

* Expected Parrot EDSL で
  「明日の夜」「朝起きた直後」「SNS 連続使用」などの
  状態シナリオをまとめて生成。([GitHub][4])
* Twin(u)（ユーザーのペルソナ）を GPT‑5.1 で実装し、
  それぞれの Nudge 候補に対して
  「どう行動するか」「感情はどうか」を回答させる。
* 得られた `s, a, simulated_reward` から、
  contextualbandits で「Twin 上の最適 Nudge ポリシー」を更新する。([GitHub][15])

### ステップ 3: 翌日のオンライン運用（RL 本番）

* 実際の決定ポイント（起床直後・夜更かし・SNS 長時間など）で
  Feature Store から状態 s を取得。
* contextualbandits の方策 πθ(s) で Nudge タイプ a を選ぶ。
* GPT‑5.1 + Moss + supermemory で、
  「その人にだけフィットした文面」を生成して通知。
* 30〜60 分の行動とユーザーフィードバックから報酬 r を計算し、
  πθ をオンライン更新。

### ステップ 4: Twin のアップデート

* 「Twin 上では ‘やめる’ と予測されたのに、現実では続けた」ケースなどを集めて
  GPT‑5.1 に「このユーザーのペルソナを調整して」と指示し、
  supermemory 上の persona テキストを更新。
* 次回の Expected Parrot 実験では、
  更新後の persona を使うので、
  Twin の挙動が現実に近づいていく。

---

## まとめ

1. Centaur は計算資源的に本番には向かないので、
   **研究用のサブ脳**に留め、
   実サービスのシミュレーション脳は GPT‑5.1 ＋ Expected Parrot にする。

2. メモリは **supermemory + Moss**、
   RL は **contextualbandits** を中核とする。

3. シミュレーションは

   * Expected Parrot で実験を定義し
   * GPT‑5.1 で Twin を動かし
   * そこで得たデータで RL を回し
   * 実世界のログで補正する
     という構造で、
     「LLM 人間シミュレーション × RL × 記憶」が一体になった形になる。

4. GitHub / 論文で出ている
   Lusifer / YuLan‑Rec / Generative Agents / AgentSociety / SkyAGI などは、
   **設計の参考・プロンプト・プロファイル更新ルールのヒントとして吸収する**が、
   コアスタックは上で決めたものに絞る。

これで、「AGI ブッダに向かう Anicca の土台」として
妥協のないスタックが一旦固まったと思う。

[1]: https://github.com/marcelbinz/Llama-3.1-Centaur-70B?utm_source=chatgpt.com "marcelbinz/Llama-3.1-Centaur-70B"
[2]: https://www.nature.com/articles/s41586-025-09215-4?utm_source=chatgpt.com "A foundation model to predict and capture human cognition"
[3]: https://featherless.ai/models/marcelbinz/Llama-3.1-Centaur-70B?utm_source=chatgpt.com "marcelbinz/Llama-3.1-Centaur-70B"
[4]: https://github.com/expectedparrot/edsl?utm_source=chatgpt.com "expectedparrot/edsl"
[5]: https://github.com/supermemoryai/supermemory?utm_source=chatgpt.com "supermemoryai/supermemory: Memory engine and app ..."
[6]: https://docs.expectedparrot.com/en/latest/starter_tutorial?utm_source=chatgpt.com "Starter Tutorial - EDSL Documentation"
[7]: https://github.com/danialebrat/Lusifer?utm_source=chatgpt.com "Lusifer: LLM-based User SImulated Feedback Environment ..."
[8]: https://www.reddit.com/r/LangChain/comments/1o080oz/mem0_vs_supermemory_numbers_on_whats_better_for/?utm_source=chatgpt.com "mem0 vs supermemory: numbers on what's better for ..."
[9]: https://www.usemoss.dev/?utm_source=chatgpt.com "Moss"
[10]: https://github.com/RUC-GSAI/YuLan-Rec?utm_source=chatgpt.com "RUC-GSAI/YuLan-Rec"
[11]: https://github.com/joonspk-research/generative_agents?utm_source=chatgpt.com "Generative Agents: Interactive Simulacra of Human Behavior"
[12]: https://huggingface.co/marcelbinz/Llama-3.1-Centaur-70B?utm_source=chatgpt.com "marcelbinz/Llama-3.1-Centaur-70B"
[13]: https://huggingface.co/meta-llama/Llama-3.1-70B?utm_source=chatgpt.com "meta-llama/Llama-3.1-70B"
[14]: https://arxiv.org/abs/2412.16984?utm_source=chatgpt.com "LLM-Powered User Simulator for Recommender System"
[15]: https://github.com/david-cortes/contextualbandits?utm_source=chatgpt.com "Python implementations of contextual bandits algorithms"
[16]: https://github.com/instadeepai/catx?utm_source=chatgpt.com "instadeepai/catx: 🐈‍⬛ Contextual bandits library for ..."
[17]: https://github.com/Persdre/llm-human-simulation?utm_source=chatgpt.com "awesome-llm-human-simulation"
