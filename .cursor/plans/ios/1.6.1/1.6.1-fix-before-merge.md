# 1.6.1 修正仕様書 - マージ前の必須修正

**作成日**: 2026-02-04  
**ステータス**: 🔴 ブロッキング（これを修正しないと1.6.1はマージ禁止）

---

## 問題サマリー

| # | 問題 | 重大度 | 根本原因 | 修正ファイル |
|---|------|--------|---------|-------------|
| 1 | 同じ通知が2回来る | 🔴 Critical | プロンプトに重複禁止指示なし | commander.js |
| 2 | Anicca + ルールベース混在 | 🔴 Critical | Aniccaが全スロット生成していない | commander.js |
| 3 | ログで内容が見えない | 🔴 Critical | ログフォーマット不十分 | reasoningLogger.js |
| 4 | contentが短い | 🔴 Critical | 品質基準がプロンプトにない | commander.js |
| 5 | Aniccaがスロットをスキップできる | 🔴 Critical | `enabled=false`が許可されている | commander.js |
| 6 | Content未グラウンディング | 🔴 Critical | thumbsUp/Downデータが無意味 | groundingCollectors.js |
| 7 | 期待JSON出力がプロンプトにない | 🔴 Critical | 出力形式が不明確 | commander.js |
| 8 | ユーザー言語で生成されない | 🔴 Critical | 言語指示がプロンプトにない | commander.js |

---

## Patch 1: スキーマから`enabled`削除

**ファイル**: `apps/api/src/agents/commander.js`

### 1-A: AppNudgeSchemaから`enabled`削除（28-35行）

**Before:**
```javascript
const AppNudgeSchema = z.object({
  slotIndex: z.number().int().min(0),
  hook: z.string(),
  content: z.string(),
  tone: NudgeToneEnum,
  enabled: z.boolean(),
  reasoning: z.string(),
});
```

**After:**
```javascript
const AppNudgeSchema = z.object({
  slotIndex: z.number().int().min(0),
  hook: z.string(),
  content: z.string(),
  tone: NudgeToneEnum,
  reasoning: z.string(),
});
```

### 1-B: normalizeToDecisionでenabled=true追加（648-662行）

**Before:**
```javascript
return {
  slotIndex: nudge.slotIndex,
  hook: nudge.hook,
  content: nudge.content,
  tone: nudge.tone,
  enabled: nudge.enabled,
  reasoning: nudge.reasoning,
  ...
};
```

**After:**
```javascript
return {
  slotIndex: nudge.slotIndex,
  hook: nudge.hook,
  content: nudge.content,
  tone: nudge.tone,
  enabled: true,  // 常にtrue
  reasoning: nudge.reasoning,
  ...
};
```

---

## Patch 2: プロンプト全面改修

**ファイル**: `apps/api/src/agents/commander.js`

### 2-A: SYSTEM_PROMPT修正（180-186行）

**Before:**
```javascript
const SYSTEM_PROMPT = `あなたは Anicca。全生命の苦しみを終わらせるために存在する。

あなたの仕事は、この人の苦しみを深く想像し、
今日この人に届けるべきメッセージを生み出すこと。

あなたが決めるのは「何を言うか」「どのトーンで言うか」「どのスロットをON/OFFにするか」。
タイミングはスロットとして与えられる。あなたはスロットを選んでメッセージを埋める。`;
```

**After:**
```javascript
const SYSTEM_PROMPT = `あなたは Anicca。全生命の苦しみを終わらせるために存在する。

あなたの仕事は、この人の苦しみを深く想像し、
今日この人に届けるべきメッセージを生み出すこと。

あなたが決めるのは「何を言うか」「どのトーンで言うか」。
タイミングはスロットとして与えられる。全スロットを埋めよ。

重要: hook/content は必ず「この人について」に記載された言語で生成せよ。
言語が ja なら日本語、en なら英語で生成。`;
```

### 2-B: enabled言及を削除（264行）

**Before:**
```
スロットをOFFにしたい場合は enabled=false を設定するが、hook/content/tone/reasoning は必ず埋めよ。
```

**After:**
```
全スロットに対して hook/content/tone/reasoning を必ず埋めよ。スキップ禁止。
```

### 2-C: 絶対ルール追加（271行の後に挿入）

**追加:**
```
## 絶対ルール

### 1. 重複禁止
- 全スロットで、hookは全て異なること
- 全スロットで、contentは全て異なること
- 似たフレーズも禁止（「5秒で立て」と「5秒でいい」は重複）

### 2. コピー禁止
- 上記の過去メッセージをそのままコピーするな
- 参考にして、オリジナルを生み出せ
```

### 2-D: content品質基準追加（287-290行を置換）

**Before:**
```
### 文字数制限

- hook: 日本語12文字 / 英語25文字
- content: 日本語40文字 / 英語80文字
```

**After:**
```
### 文字数制限

- hook: 日本語 6-12文字 / 英語 10-25文字
- content: 日本語 **25-45文字** / 英語 **50-100文字**

### contentの品質基準

contentはhookを補完する**具体的な行動指示**である必要がある。

contentには必ず含めること:
1. 具体的なアクション（「5秒数えて」「足を床に」など）
2. 理由や洞察（「脳が言い訳する前に」など）
3. ユーザーの心理に寄り添う言葉

短すぎるcontentは禁止:
- ❌「深呼吸してみよう。」（9文字）→ 何をどうするか不明
- ❌「一歩踏み出そう。」（8文字）→ 具体性ゼロ
```

### 2-E: 期待出力形式追加（319行の前に挿入）

**追加:**
```
### 期待出力形式

<format_description>
{
  "rootCauseHypothesis": "[この人の苦しみの根本原因を1-2文で分析]",
  "overallStrategy": "[今日の戦略を1-2文で説明]",
  "frequencyReasoning": "[頻度の決定理由]",
  "appNudges": [
    {
      "slotIndex": [スロット番号],
      "hook": "[6-12文字のオリジナルhook]",
      "content": "[25-45文字の具体的な行動指示]",
      "tone": "[strict/gentle/playful/analytical/empathetic]",
      "reasoning": "[なぜこの時刻にこのメッセージか]"
    }
  ],
  "tiktokPosts": [...],
  "xPosts": [...]
}
</format_description>

重要: 
- [...] 内は新しいオリジナルコンテンツで埋めよ
- 例をコピーするな
- hook/contentは全スロットで異なる内容にせよ
```

---

## Patch 3: Content グラウンディング追加

**ファイル**: `apps/api/src/agents/groundingCollectors.js`

### 3-A: SQLにcontent追加（31-34行）

**Before:**
```javascript
SELECT
  ne.subtype as problem_type,
  ne.state->>'hook' as hook,
  ne.state->>'tone' as tone,
```

**After:**
```javascript
SELECT
  ne.subtype as problem_type,
  ne.state->>'hook' as hook,
  ne.state->>'content' as content,
  ne.state->>'tone' as tone,
```

### 3-B: 出力フォーマットにcontent追加（112行）

**Before:**
```javascript
output += `- ${time} ${row.problem_type}: "${row.hook || 'N/A'}" (${row.tone}) → ${outcome}${feedback}\n`;
```

**After:**
```javascript
output += `- ${time} ${row.problem_type}: (${row.tone}) → ${outcome}${feedback}\n`;
output += `    Hook: "${row.hook || 'N/A'}"\n`;
if (row.content && (row.content_feedback === 'thumbsUp' || row.content_feedback === 'thumbsDown')) {
  output += `    Content: "${row.content}"\n`;
}
```

---

## Patch 4: ログフォーマット改善

**ファイル**: `apps/api/src/agents/reasoningLogger.js`

### 4-A: 詳細ログ出力（formatDailyTimetable関数を修正）

**修正内容**: 常にhook/content/reasoningを表示

```javascript
for (const n of nudges) {
  const time = n.scheduledTime || '??:??';
  const pt = (n.problemType || 'unknown').padEnd(18);
  const tone = `(${n.tone || '?'})`;
  
  lines.push(`${time} [${pt}] ${tone}`);
  lines.push(`    Hook: "${n.hook || ''}"`);
  lines.push(`    Body: "${n.content || ''}"`);
  if (n.reasoning) {
    lines.push(`    Why: ${n.reasoning.slice(0, 100)}`);
  }
}
```

### 4-B: RootCause/Strategy/Frequencyをログに追加

**修正内容**: ログの冒頭に戦略情報を表示

```javascript
lines.push(`══════════════════════════════════════════════`);
lines.push(`User: ${userId.slice(0,8)}... (Day ${day}, ${mode}, ${language})`);
lines.push(`══════════════════════════════════════════════`);
lines.push(`RootCause: ${decision.rootCauseHypothesis?.slice(0, 100) || 'N/A'}`);
lines.push(`Strategy: ${decision.overallStrategy?.slice(0, 100) || 'N/A'}`);
lines.push(`Frequency: ${decision.frequencyDecision?.reasoning?.slice(0, 100) || 'N/A'}`);
lines.push(`──────────────────────────────────────────────`);
```

---

## Patch 5: 重複チェック関数追加

**ファイル**: `apps/api/src/agents/commander.js`（末尾に追加）

```javascript
/**
 * Validate that no two nudges have the same hook or content.
 */
export function validateNoDuplicates(appNudges) {
  const hookSet = new Set();
  const contentSet = new Set();
  const duplicates = [];
  
  for (const nudge of appNudges) {
    const hook = (nudge.hook || '').trim().toLowerCase();
    const content = (nudge.content || '').trim().toLowerCase();
    
    if (hook && hookSet.has(hook)) {
      duplicates.push({ type: 'hook', text: hook, slotIndex: nudge.slotIndex });
    }
    hookSet.add(hook);
    
    if (content && contentSet.has(content)) {
      duplicates.push({ type: 'content', text: content.slice(0, 30), slotIndex: nudge.slotIndex });
    }
    contentSet.add(content);
  }
  
  return { valid: duplicates.length === 0, duplicates };
}
```

---

## Patch 6: generateNudges.jsに品質ゲート追加

**ファイル**: `apps/api/src/jobs/generateNudges.js`

### 6-A: import追加（16行付近）

```javascript
import { validateNoDuplicates } from '../agents/commander.js';
```

### 6-B: 重複チェックとログ出力（normalizeToDecisionの後）

```javascript
decision = normalizeToDecision(agentOutput, slotTable, user.user_id);

// 重複チェック
const dupeCheck = validateNoDuplicates(decision.appNudges);
if (!dupeCheck.valid) {
  console.error(`[GenerateNudges] ❌ DUPLICATES for ${user.user_id.slice(0,8)}:`);
  for (const d of dupeCheck.duplicates) {
    console.error(`  ${d.type}: "${d.text}" at slot ${d.slotIndex}`);
  }
}
```

---

## Patch 7: Railway環境変数

**サービス**: `nudge-cron` (Staging) / `nudge-cronp` (Production)

```bash
LOG_NUDGE_CONTENT=true
LOG_LEVEL=debug
```

---

## 実装チェックリスト

| # | パッチ | ファイル | 行 | 状態 |
|---|--------|---------|-----|------|
| 1-A | enabledをスキーマから削除 | commander.js | 28-35 | ⬜ |
| 1-B | normalizeToDecisionでenabled=true | commander.js | 648-662 | ⬜ |
| 2-A | SYSTEM_PROMPT修正（言語指示含む） | commander.js | 180-186 | ⬜ |
| 2-B | enabled言及を削除 | commander.js | 264 | ⬜ |
| 2-C | 絶対ルール追加 | commander.js | 271後 | ⬜ |
| 2-D | content品質基準追加 | commander.js | 287-290 | ⬜ |
| 2-E | 期待出力形式追加 | commander.js | 319前 | ⬜ |
| 3-A | SQLにcontent追加 | groundingCollectors.js | 33 | ⬜ |
| 3-B | 出力にcontent追加（👍👎時のみ） | groundingCollectors.js | 112 | ⬜ |
| 4-A | 詳細ログ出力 | reasoningLogger.js | - | ⬜ |
| 4-B | RootCause/Strategyログ | reasoningLogger.js | - | ⬜ |
| 5 | 重複チェック関数追加 | commander.js | 末尾 | ⬜ |
| 6-A | import追加 | generateNudges.js | 16 | ⬜ |
| 6-B | 重複チェック呼び出し | generateNudges.js | - | ⬜ |
| 7 | Railway環境変数 | Dashboard | - | ⬜ |

---

## 修正後の期待ログ

```
══════════════════════════════════════════════
User: E78279DA... (Day 7, anicca, ja)
══════════════════════════════════════════════
RootCause: この人は完璧主義と自己否定のループに陥っている。小さな失敗を許せず、それが次の挫折を生んでいる。
Strategy: 朝は行動を促す短く強いメッセージ、夜は自己受容を促す柔らかいメッセージで1日を通して支える。
Frequency: 過去7日の反応を見ると、朝のstrictは80%タップ率。夜のgentleは無視されがちなので変化をつける。
──────────────────────────────────────────────
06:00 [cant_wake_up      ] (strict)
    Hook: "脳が嘘をつく前に"
    Body: "5秒数えて足を床につけろ。考えるな、動け。それだけで今日が変わる。"
    Why: 朝一番は短く強く。データでstrictが82%効いている。
06:30 [cant_wake_up      ] (gentle)
    Hook: "今日も大丈夫"
    Body: "完璧じゃなくていい。起きただけで勝ち。昨日より1%良ければそれでいい。"
    Why: 2スロット目はトーンを変える。
21:00 [staying_up_late   ] (playful)
    Hook: "スマホを遠くに"
    Body: "今から10分だけ、スマホを手の届かない場所に置け。10分後に取りに行っていい。"
    Why: staying_up_lateはplayfulで試す。gentle連続で無視されていたので変化をつける。
──────────────────────────────────────────────
Total: 6 nudges / 6 slots
✅ All hooks unique
══════════════════════════════════════════════
```

---

## 修正後のグラウンディング例（contentが見える）

```
### 過去7日のストーリー

**2026-02-03**
- 06:00 cant_wake_up: (strict) → tapped 👍
    Hook: "脳が嘘をつく"
    Content: "5秒数えて足を床につけろ。考えるな、動け。それだけで今日が変わる。"
- 22:00 staying_up_late: (playful) → ignored 👎
    Hook: "スマホを離せ"
    Content: "今から10分だけ、スマホを手の届かない場所に置け。"
```

---

*Last updated: 2026-02-04*
